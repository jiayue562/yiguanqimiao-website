# 龙心 OS 自主学习系统 - 完整指令手册

> 📚 四层学习能力架构 · 9 条核心指令

---

## 📋 系统架构

```
第四层：自主进化（定时整理 + 知识蒸馏）
              ↑
第三层：多格式读取（markitdown + 文档技能）
              ↑
第二层：实时获取（网络搜索 + 公众号检索）
              ↑
第一层：持久记忆（ima 笔记 + Obsidian + MEMORY.md）
              ↑
    【基础层：已有能力——推理、分析、生成、代码、研究】
```

---

## 🏗️ 第一层：持久记忆系统

### 指令一：写入记忆

```
帮我把以下内容写入长期记忆：[你想记住的内容，比如：我的项目是 XX，我的偏好是 XX，我学到了 XX]
写入路径：MEMORY.md
```

**用途**: 把重要信息、偏好、结论永久保存，下次对话直接调用。

**实现方式**:
- 使用 `edit` 工具追加到 `MEMORY.md`
- 自动分类到对应板块
- 添加时间戳和上下文

**示例**:
```
帮我把以下内容写入长期记忆：悟空偏好直接、清晰的沟通风格，不喜欢客套话
```

---

### 指令二：调取记忆

```
请读取我的长期记忆（MEMORY.md），告诉我关于 [主题] 你记住了什么？
```

**用途**: 随时调出你之前存入的记忆，验证系统记住了什么。

**实现方式**:
- 使用 `memory_search` 语义搜索
- 使用 `memory_get` 精确读取
- 输出结构化摘要

**示例**:
```
请读取我的长期记忆，告诉我关于"项目偏好"你记住了什么？
```

---

### 指令三：记忆蒸馏

```
请读取我的 MEMORY.md，找出其中重复、过时、冗余的内容，压缩整理成更精炼的版本，保留所有核心信息，去掉废话。
```

**用途**: 记忆积累多了之后定期清理，保持精炼可用。**建议每月执行一次。**

**实现方式**:
1. 读取完整 MEMORY.md
2. 识别重复内容
3. 标记过时信息
4. 压缩整理
5. 生成新版本

**执行频率**: 每月一次（可配置 cron 自动执行）

---

## 🌐 第二层：实时知识获取

### 指令四：联网搜索（单话题）

```
帮我搜索关于 [话题] 的最新信息，输出格式：结论优先，数据来源后置，直接可行动的洞察。
```

**用途**: 快速获取某个话题的最新信息，结果直接可用。

**可用技能**:
- `web-search-plus` (7 引擎智能路由)
- `multi-search-engine` (17 引擎聚合)
- `cn-web-search` (13+ 中国搜索引擎)

**输出格式**:
```
## 核心结论
[1-3 句直接答案]

## 关键数据
- 数据点 1 (来源)
- 数据点 2 (来源)

## 行动建议
1. [具体行动]
2. [具体行动]
```

---

### 指令五：深度研究（多角度）

```
对 [话题] 进行深度研究，从以下维度分析：
1. 现状与趋势
2. 核心数据与案例
3. 对我的直接影响
4. 行动建议
```

**用途**: 需要全面了解一个领域时使用，比指令四更系统深入。

**执行流程**:
1. 多引擎搜索获取广度
2. 深度阅读关键页面
3. 交叉验证信息
4. 结构化输出

**输出格式**:
```
# [话题] 深度研究报告

## 1. 现状与趋势
[当前状态 + 发展方向]

## 2. 核心数据与案例
[关键数据 + 典型案例]

## 3. 对我的直接影响
[与龙心 OS/悟空的关联]

## 4. 行动建议
[具体、可执行的步骤]
```

---

## 📂 第三层：文件与知识库读取

### 指令六：读取本地文件

```
请读取以下文件，提炼核心内容：
文件路径：[你的文件路径]
输出：核心结论 + 关键信息 + 行动建议
```

**用途**: 读取你电脑上的文档，直接提炼精华。

**支持格式**:
- **文本**: `.md`, `.txt`, `.json`, `.yaml`
- **Office**: `.docx`, `.xlsx`, `.pptx`
- **PDF**: `.pdf` (需 markitdown)
- **图片**: `.jpg`, `.png`, `.webp` (OCR)
- **音频**: `.mp3`, `.wav` (转录)

**已安装技能**:
- `read` (基础文件读取)
- `excel-xlsx-1.0.2` (Excel 处理)
- `word-docx-1.0.2` (Word 处理)
- `powerpoint-pptx-1.0.1` (PPT 处理)
- `pdf-to-image-preview-0.1.0` (PDF 转图片)
- `vision-3.5.0` (图像处理)
- `openai-whisper` (语音转文字)

**待安装**:
- `markitdown` (统一文档转换)

---

### 指令七：批量读取知识库

```
读取我的知识库中，关于 [主题] 的所有笔记（路径：[你的知识库路径]），综合整理后输出结构化知识图谱。
输出格式：
1. 总体架构图
2. 核心结论（直接可用）
3. 知识节点关系图
4. 行动建议
```

**用途**: 批量读取 Obsidian 或其他本地知识库，整合成结构化知识图谱。

**知识库路径**:
- **Obsidian**: `D:\以观其妙书院知识库\观其妙书院`
- **OpcCla**: `C:\Users\jia'yue\.OpcCla 知识库`
- **OpenClaw**: `C:\Users\jia'yue\.openclaw\workspace`

**实现方式**:
1. 扫描指定路径下所有 `.md` 文件
2. 语义搜索与主题相关的内容
3. 提取实体和关系
4. 生成知识图谱 (Mermaid)
5. 综合输出结构化报告

---

## ⚙️ 第四层：自主进化机制

### 指令八：每日知识沉淀

```
今天的对话结束前，帮我做一次知识沉淀：
1. 今天学到了什么新知识？
2. 有哪些结论值得长期保留？
3. 有哪些偏好/方法被确认或修正？
把有价值的内容写入 MEMORY.md。
```

**用途**: 每次重要对话结束前执行，把当日收获结晶化，不让好内容随对话消散。

**实现方式**:
1. 回顾当日会话记录
2. 提取新知识、新结论
3. 识别确认/修正的偏好
4. 写入 MEMORY.md 对应板块
5. 创建/更新 memory/YYYY-MM-DD.md

**触发时机**:
- 重要项目讨论后
- 学习新知识后
- 做出关键决策后
- 每日会话结束前

---

### 指令九：设置定时学习任务

```
帮我设置一个每周一早上 9 点自动运行的任务：读取过去 7 天的工作记忆，整理成本周知识摘要，写入 MEMORY.md 的"周摘要"板块。
```

**用途**: 设置一次，之后每周自动执行。系统在你不用它的时候也在工作。

**实现方式**:
```bash
openclaw cron add --schedule "0 9 * * 1" --command "weekly-knowledge-summary"
```

**任务内容**:
1. 读取 memory/ 目录下过去 7 天的日常笔记
2. 提取关键事件、决策、学习点
3. 生成周摘要（结构化格式）
4. 追加到 MEMORY.md 的"周摘要"板块
5. 可选：同步到 Obsidian 和 IMA

** cron 表达式参考**:
| 频率 | 表达式 | 说明 |
|------|--------|------|
| 每日 | `0 2 * * *` | 每天凌晨 2 点 |
| 每周 | `0 9 * * 1` | 每周一上午 9 点 |
| 每月 | `0 10 1 * *` | 每月 1 日上午 10 点 |

---

## 📌 使用顺序建议

```
第一次使用 → 先发指令一（写入你的基本信息）
                    ↓
        日常使用 → 指令四/五（获取知识）+ 指令六/七（读取文件）
                    ↓
        每次结束前 → 指令八（知识沉淀）
                    ↓
        系统维护 → 指令三（每月蒸馏一次）
                    ↓
        自动化 → 指令九（一次设置，永久运行）
```

---

## 💡 三句话理解这个系统

1. **指令 1-3 = 让龙龟记住你，不再每次从零开始**
   - 写入你的偏好、项目、决策
   - 随时调取验证
   - 定期清理保持精炼

2. **指令 4-7 = 让龙龟帮你获取和整理知识**
   - 实时搜索最新信息
   - 深度研究复杂话题
   - 读取各种格式文件
   - 整合知识库内容

3. **指令 8-9 = 让系统自动进化，不需要你操心**
   - 每次对话自动沉淀精华
   - 每周自动整理摘要
   - 系统在你不用它时也在工作

---

## 🔧 技能安装清单

### 已安装 ✅

| 技能 | 功能 | 层级 |
|------|------|------|
| `ima-skill` | IMA 知识库读写 | Layer 1 |
| `notesmd` | Obsidian 笔记管理 | Layer 1 |
| `qclaw-knowledge-sync` | 三向同步 | Layer 1 |
| `web-search-plus` | 统一搜索 (7 引擎) | Layer 2 |
| `multi-search-engine` | 17 引擎聚合 | Layer 2 |
| `cn-web-search` | 中文搜索 | Layer 2 |
| `excel-xlsx-1.0.2` | Excel 处理 | Layer 3 |
| `word-docx-1.0.2` | Word 处理 | Layer 3 |
| `powerpoint-pptx-1.0.1` | PPT 处理 | Layer 3 |
| `pdf-to-image-preview` | PDF 转图片 | Layer 3 |
| `vision-3.5.0` | 图像处理 | Layer 3 |
| `openai-whisper` | 语音转文字 | Layer 3 |
| `self-improving-agent` | 自我优化 | Layer 4 |
| `cron-helper` | 定时任务 | Layer 4 |

### 待安装 ⏳

| 技能 | 功能 | 层级 | 优先级 |
|------|------|------|--------|
| `markitdown` | 统一文档转换 | Layer 3 | 🔴 高 |
| `summarize` | 长内容摘要 | Layer 2 | 🟡 中 |
| `wechat-article-search` | 公众号搜索 | Layer 2 | 🟡 中 |

---

## 🎯 快速开始

### 第一次使用（5 分钟）

1. **写入基本信息** (指令一)
   ```
   帮我把以下内容写入长期记忆：
   - 我的名字：悟空
   - 我的项目：龙心 OS
   - 我的偏好：直接、清晰、有深度的沟通
   ```

2. **验证记忆** (指令二)
   ```
   请读取我的长期记忆，告诉我关于"项目"你记住了什么？
   ```

3. **设置自动化** (指令九)
   ```
   帮我设置每周一早上 9 点的周知识摘要任务
   ```

### 日常使用流程

```
早上 → 指令四/五：获取最新信息
       ↓
工作中 → 指令六/七：读取文件/知识库
       ↓
对话结束 → 指令八：知识沉淀
       ↓
每周五 → 指令三：记忆蒸馏（每月一次）
```

---

## 📊 能力成熟度评估

| 层级 | 能力 | 状态 | 完成度 |
|------|------|------|--------|
| Layer 1 | 持久记忆 | ✅ 已配置 | 100% |
| Layer 2 | 实时获取 | ✅ 已配置 | 90% |
| Layer 3 | 文件读取 | ✅ 已配置 | 85% |
| Layer 4 | 自主进化 | ✅ 已配置 | 80% |

**整体完成度**: 90% ✅

---

_龙心 OS 自主学习系统 v1.0 · 最后更新：2026-04-13 · 悟空 × 龙龟 共同维护_
